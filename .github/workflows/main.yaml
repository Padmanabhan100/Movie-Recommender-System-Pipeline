name: Build and Deploy to Cloud Run

on:
  push:
    branches: [ "master" ]

env:
  PROJECT_ID: zippy-bee-332908 # TODO: update Google Cloud project id
  GAR_LOCATION: movieflix # TODO: update Artifact Registry location
  SERVICE: my-cloud-run-service # TODO: update Cloud Run service name
  REGION: us-east1 # TODO: update Cloud Run service region

jobs:
  # train-and-report:
  #   runs-on: ubuntu-latest
  #   container: docker://ghcr.io/iterative/cml:0-dvc2-base1
  #   steps:
  #     - uses: actions/checkout@v3
  #     - run: |
  #         pip install --upgrade pip
  #         echo "Ran the upgrades!"

  #         pip install -r requirements.txt
  #         echo 'Installed the requirements successfully!'
          
  #         # Run the stages
  #         dvc repro

  #         # Fetch the old git history
  #         #git fetch --prune

  #         # Compare the current metrics with the earlier one
  #         #dvc metrics diff master > report.md

  #         # markdown the plot image in report.md and then comment it on github
  #         #echo '![](./confusion_matrix.png "Confusion Matrix")' >> report.md
  #         #cml comment create report.md
          
  #       env:
  #         REPO_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  deploy:
    # Add 'id-token' with the intended permissions for workload identity federation
    permissions:
      contents: 'read'
      id-token: 'write'

    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2

      # NOTE: Alternative option - authentication via credentials json
      - name: Google Auth
        id: auth
        uses: 'google-github-actions/auth@v0'
        with:
          credentials_json: '${{ secrets.GCP_CREDENTIALS }}'


      # BEGIN - Docker auth and build (NOTE: If you already have a container image, these Docker steps can be omitted)

      # Authenticate Docker to Google Cloud Artifact Registry
      - name: Docker Auth
        id: docker-auth
        uses: 'docker/login-action@v1'
        with:
          username: 'oauth2accesstoken'
          password: '${{ steps.auth.outputs.access_token }}'
          registry: '${{ env.GAR_LOCATION }}-docker.pkg.dev'

      - name: Build and Push Container
        run: |-
          docker build -t "${{ env.GAR_LOCATION }}-docker.pkg.dev/${{ env.PROJECT_ID }}/${{ env.SERVICE }}:${{ github.sha }}" ./
          docker push "${{ env.GAR_LOCATION }}-docker.pkg.dev/${{ env.PROJECT_ID }}/${{ env.SERVICE }}:${{ github.sha }}"

      # END - Docker auth and build

      - name: Deploy to Cloud Run
        id: deploy
        uses: google-github-actions/deploy-cloudrun@v0
        with:
          service: ${{ env.SERVICE }}
          region: ${{ env.REGION }}
          # NOTE: If using a pre-built image, update the image name here
          image: ${{ env.GAR_LOCATION }}-docker.pkg.dev/${{ env.PROJECT_ID }}/${{ env.SERVICE }}:${{ github.sha }}

      # If required, use the Cloud Run url output in later steps
      - name: Show Output
        run: echo ${{ steps.deploy.outputs.url }}